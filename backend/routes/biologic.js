// routes/biologic.js - rute pentru Date biologice / paraclinic (tabelul biologic).
// Markeri inflamatori, status imunologic.
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/biologic", require("./routes/biologic"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru date biologice

module.exports = router;
