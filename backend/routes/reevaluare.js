// routes/reevaluare.js - rute pentru Reevaluare (tabelul reevaluare).
// Datele de reevaluare periodica a pacientului.
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/reevaluare", require("./routes/reevaluare"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru reevaluare

module.exports = router;
