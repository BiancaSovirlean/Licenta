// routes/explorariFunctionale.js - rute pentru Explorari functionale (tabelul explorari_functionale).
// Spirometrie (FVC, FEV1, FEV1/FVC), DLCO, test de mers 6 minute (6MWT).
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/explorari-functionale", require("./routes/explorariFunctionale"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru explorari functionale

module.exports = router;
