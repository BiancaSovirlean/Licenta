// routes/tratament.js - rute pentru Tratament respirator & vaccinare (tabelul tratament).
// Echipamente (oxigenoterapie, ventilatie), medicatie inhalatorie/orala, fizioterapie.
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/tratament", require("./routes/tratament"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru tratament

module.exports = router;
