// routes/exacerbari.js - rute pentru Istoric exacerbari (tabelul exacerbari).
// Numar pe an, durata, severitate, agent patogen implicat.
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/exacerbari", require("./routes/exacerbari"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru exacerbari

module.exports = router;
