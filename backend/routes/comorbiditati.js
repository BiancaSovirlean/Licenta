// routes/comorbiditati.js - rute pentru Comorbiditati (tabelul comorbiditati).
// Boli asociate: pulmonare, ORL, cardiovasculare, neurologice etc.
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/comorbiditati", require("./routes/comorbiditati"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru comorbiditati

module.exports = router;
