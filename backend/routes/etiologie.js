// routes/etiologie.js - rute pentru Etiologie (tabelul etiologie).
// Schelet gol: adauga aici rutele (GET, POST...) cand lucram la aceasta sectiune.
// Ca sa devina activ, in server.js: app.use("/etiologie", require("./routes/etiologie"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru etiologie

module.exports = router;
