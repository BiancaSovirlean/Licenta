// routes/bacteriologie.js - rute pentru Bacteriologie (tabelul bacteriologie).
// Produse biologice (sputa, aspirat, LBA), germeni identificati, decizii de tratament/eradicare.
// Schelet gol: adauga aici rutele cand lucram la aceasta sectiune.
// In server.js: app.use("/bacteriologie", require("./routes/bacteriologie"));

const express = require("express");
const router = express.Router();
const pool = require("../db");

// TODO: rute pentru bacteriologie

module.exports = router;
