// components/grafice/TimelineExacerbari.jsx - linia timpului cu exacerbari / depistari de bacterii.
// Refolosibil in fisa pacientului si in dashboard.
// Schelet gol: continutul vine cand lucram la grafice (vom folosi o biblioteca de grafice).

function TimelineExacerbari() {
  return <div>Timeline exacerbari (in lucru)</div>;
}

export default TimelineExacerbari;
